# 2026-08-31

## Issue #94

Added the Touchline-owned evaluator for the approved 09.01 contemporary lineup-evidence protocol. The evaluator consumes #93 joined records through a narrow input projection, applies chronology-safe population exclusions, computes provider Brier/log loss and fixed-bin calibration, summarizes the three approved evidence classes, fits unpenalized logistic models with player-clustered robust covariance, compares descriptive in-sample fitted Brier scores, applies explicit sufficiency/conclusion gates, and translates numerical failures into bounded diagnostics.

Added `statsmodels` and explicit `numpy` runtime dependencies; the lockfile resolves statsmodels 0.15.0. Deterministic synthetic tests cover population reconciliation, fixed metrics and boundaries, clustering, replay, sample gates, separation/covariance failure, and approved conclusion states. No forecast, expected-minutes, xPts, optimiser, #92, #93, or #95 semantics were changed.

## Issue #95

Added a typed, deterministic lineup-evidence validation artefact built from complete #93 joined records. The output preserves full projection, evidence, chronology, and official outcome provenance, embeds the unchanged #94 result, separates full dataset identity from #94 evaluator identity, supports canonical JSON persistence and replay-safe rendering, and uses no RunRecord lifecycle coupling or new dependency. Completed schema-version-aware dataset identity, centralized semantic integrity hashes, parse/load tamper rejection including content-addressed filenames, and missing-outcome provider-version preservation; Ruff, Pyright, and 754 synthetic tests pass.
