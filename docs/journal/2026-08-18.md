# Journal Entry - 2026-08-18

## Issue #57: Structured Team-News Evidence Ingestion

### Overview
Implemented the structured team-news evidence provider for pre-deadline review of football evidence. The provider ingests immutable external football evidence in a structured JSON format and produces availability evidence for the FPL decision engine.

### Changes Made

#### Production Code
- **`src/fpl_decision_engine/infrastructure/providers/team_news/structured.py`**:
  - `StructuredTeamNewsEvidenceProvider` class with constructor and `evidence()` method
  - `_StructuredTeamNewsRecordV1` and `_StructuredTeamNewsArtifactV1` pydantic models
  - Schema validation (schema_version=1, timezone-aware timestamps, valid domain enums)
  - Exact ExternalRef player mapping with ambiguity detection
  - Deterministic mapping fingerprint (SHA-256 over sorted mappings)
  - AvailabilityEvidence output with provenance tracking
  - Provider protocol satisfaction (NewsEvidenceProvider, NEWS_EVIDENCE capability)

#### Test Fixtures
- **`tests/fixtures/team_news/structured_evidence_v1.json`**: Deterministic synthetic test data with 3 evidence records covering doubtful/unavailable/available states

#### Infrastructure Tests (16 tests)
- Valid artefact mapping to canonical AvailabilityEvidence
- NewsEvidenceProvider protocol satisfaction
- Timestamp handling (published_at, observed_at, processed_at)
- Source reference and attributes retention
- SHA-256 hash verification
- Deterministic mapping fingerprint
- Missing published_at handling
- Unmapped/ambiguous external identity errors
- Malformed JSON rejection
- Unsupported schema version rejection
- Naive timestamp rejection
- Published_at > observed_at rejection
- Duplicate evidence IDs rejection
- Invalid enum values rejection
- Player filtering determinism

#### Integration Tests (6 tests)
- Newer definitive unavailability -> EXCLUDE; legal recommendation possible
- DecisionRun provenance survives (provider_id, provider_version, retrieved_at, snapshot_id, source_sha256, mapping_fingerprint, season)
- Positive no changes to projection metadata
- Stale evidence retained no decision effect
- Contradictory latest evidence -> CONFLICT, review, no exclusion
- Basic availability assessment

#### Journal Entry
- **`docs/journal/2026-08-18.md`**: Documentation of implementation work

### Quality Gates (with #57 files restored)
- `uv run ruff check .` - Passes (after fixing test file import/issues)
- `uv run pyright` - 0 errors, 0 warnings
- `uv run pytest tests/` - 194 passed (including all #57 tests)

### Design Constraints Honoured
- No forecast recalibration, xPts adjustments, expected-minutes adjustment
- No start/appearance probability adjustment
- No source reliability scores, source precedence/ranking
- No generic evidence/provider frameworks, provider registries/plugins
- No web crawling, scraping, network calls, LLM interpretation
- Reuses existing assess_availability() and apply_availability_exclusions() contracts unchanged
