# Architecture Decision Records

Architecture Decision Records (ADRs) capture significant technical decisions and their rationale.

Each ADR is immutable once accepted. If a decision changes, add a new ADR that supersedes the old one rather than silently rewriting history.
