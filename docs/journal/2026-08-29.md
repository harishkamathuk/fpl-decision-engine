# 2026-08-29

## Issue #88: bounded submission safety gate

Implemented a small application-level safety seam for pre-execution planning and fresh post-execution verification. It blocks unverified/current-source failures, validates manager/Gameweek and FINAL_DECISION references, requires acknowledgement for explicitly supplied previous-state differences, compares intended selection semantics deterministically, and derives ACTUAL_CHOICE from the observed post-execution state.

No acquisition, live mutation, optimiser, chip, finance acquisition, RunRecord, orchestrator, or general override changes were made.

### Reverse-mapping ordering fix

`_actual_choice()` previously reconstructed `SubmittedDecision` by reverse-mapping observed FPL element IDs to canonical UUIDs in the element-ID order seen on the observed snapshot. Because `DecisionSelection` requires canonical `str(uuid)` ordering for `squad_ids` and `starting_xi_ids`, any observed snapshot whose element-ID order diverged from UUID order produced an identity that either failed validation or silently lost canonical ordering. `_actual_choice()` now sorts the reverse-mapped `squad_ids` and `starting_xi_ids` by `str(uuid)`, while leaving `bench_ids` order, captain/vice identities, and the explicit reverse-mapping seam untouched. Tests were converted from `UUID(int=...)` inference to deterministic canonical UUIDv5 identities and now cover identical post-exec state, canonical identity preservation, squad/XI permutations, outfield-bench reorder, and fail-closed missing/duplicate mappings.

## Issue #88: canonical safety decision identity

Replaced the submission-safety Python `repr` identity with the existing canonical DecisionBundle artefact SHA-256. New safety artefacts use schema/kind v2 and bind PRE/POST replay to the exact recorded DecisionBundle; historical v1 evidence remains readable but is rejected as authoritative resume proof. No RunRecord schema, safety semantics, acquisition, mapping, operator-confirmation, or live-mutation behavior changed.
