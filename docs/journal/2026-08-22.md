# 2026-08-22

## Context

Issue #80 settled the control-plane architecture contract for Epic #73; Issue #81
(typed run-record provenance ledger) is the first unblocked implementation slice. The
operational problem it removes: Gameweek provenance was maintained by manual JSON
edits (`jq > tmp && mv` style), which is error-prone, non-atomic and lets invalid
stage/state transitions slip through. This session implemented #81 and its tests.

## What changed

- New typed `RunRecord` domain model (`domain/run_record.py`) encoding the #80
  invariants directly in validation: stage states PENDING/RUNNING/PASS/WARN/FAIL/BLOCKED,
  run states provisional/completed/failed/authoritative, append-only stage attempts,
  artefact references + SHA-256 hashes, recorded decisions, authority approval events.
- New `RunRecordRepository` port and error hierarchy (`ports/run_records.py`).
- New `RunRecordLedger` (`infrastructure/persistence/run_records.py`): one
  schema-validated JSON document per run under `state/run-records/`, written atomically
  (temp file + fsync + `os.replace`), with optimistic concurrency detection via the raw
  document snapshot the caller loaded. Legacy documents without `schema_version` are
  read best-effort: present fields parsed, absent fields stay absent, unparseable fields
  reported in `parse_issues`; nothing is fabricated.
- New `RunRecordService` (`application/run_record_service.py`) implementing the approved
  transitions, closure validation against the #80 definitions of completed/failed, and
  promotion with explicit attributable approval. Retries append new attempts and never
  rewrite prior ones.
- New `touchline` CLI (`touchline_cli.py`, `touchline` script in `pyproject.toml`) with
  `run-record` create/show/list/stage/artefact/decision/close/promote/validate commands.
- Tests: domain model invariants, service scenarios (all 12 required by #81 plus
  promotion/supersession/retry/legacy), ledger atomicity/corruption/resolution, CLI smoke.
- Dev-only `tzdata` dependency added: the existing Parquet round-trip tests panic in
  sandboxes without a system tz database (`ZoneInfoNotFoundError: UTC`); `tzdata`
  supplies the static database and is not a runtime dependency.

## Decisions

- **Previous-run authority**: explicit `previous_run_id` must reference a recorded run
  or creation is rejected with an actionable diagnostic. An omitted id deterministically
  resolves the current authoritative run for the same (season, Gameweek) — matching #80's
  "at most one current authoritative run per season/Gameweek" keying. Cross-Gameweek
  lineage therefore requires an explicit id (operator choice), not a heuristic chain.
  Resolution reads authority-event timestamps from document content; mtime/directory
  ordering is never used. Tied approval timestamps raise an explicit ambiguity error
  instead of guessing.
- **Completion semantics** follow #80 literally: completed requires every mandatory
  stage (declared on the record at creation) to have a terminal PASS/WARN latest attempt
  and no mandatory FAIL/BLOCKED remaining; failed requires mandatory execution to have
  failed. A run that is neither cannot be closed — it stays provisional rather than
  inventing an "abandoned" state.
- **Retry after PASS/WARN is rejected** with an actionable message. #80 permits explicit
  rerun of successful stages only where the stage contract is safely repeatable; whether
  a stage is repeatable belongs to the future DAG/orchestrator (#84), so the ledger
  refuses rather than choosing semantics.
- **Legacy vs v1** is distinguished by `schema_version`: absent -> legacy best-effort
  read; `1` -> strict validated read; `>1` -> `UnsupportedSchemaVersion`. Legacy records
  are readable but not silently migrated; mutations reject them and point the operator
  at recreating through the typed interface.

## Risks / open questions

- JSON-file ledger has no cross-process locking; concurrent edits by two processes are
  detected (read-modify-write compare) rather than silently lost, but the loser must
  retry. Fine for the single-operator local-first model; revisit if the orchestrator
  (#84) makes concurrent writers realistic.
- Same-(season, Gameweek) resolution scope could be re-read by the owner as "previous
  Gameweek". I implemented the literal pairing with the #80 per-season/Gameweek
  authority invariant; weekly chaining policy is 06.02's call. Flagged in the #81 handoff.
- `state` is stored and validated against history on every read, so hand-edited files
  that contradict their own history are rejected rather than silently repaired.

## Next

Hand #81 to 09.xx for regression/compatibility validation. #82 (`touchline doctor`) can
proceed in parallel; #83 evidence identity can build on the ledger interface once
agreed; #84 orchestrator should wait for #81/#83.

## Issue #83 implementation findings

Implemented the #80 evidence boundary as a typed v1 Gameweek manifest rather than an
archive or database. The semantic SHA-256 covers season/Gameweek, snapshot/provider
identity, semantic source-as-of time, exact bootstrap/fixtures/snapshot byte hashes,
projection content identity, semantic forecast generation/effective time,
provider/source/model version and only upstream lineage actually supplied. Manifest
JSON formatting and key order are non-semantic; source artefact whitespace is part of
the exact bytes and therefore changes content and evidence identities.

Acquisition UUID and retrieval timestamps remain separate provenance. Filesystem
references locate immutable bytes for audit but do not enter semantic identity; mtimes
and directory order are never read. Projection upstream lineage is an unordered set of
uniquely named attributes and is canonicalised by name/value before hashing.

Manifest publication now uses the repository's same-filesystem temporary-file plus
atomic hard-link/no-replace pattern. A concurrent identical winner is idempotent; a
different-byte winner is rejected and never overwritten. Temporary files are cleaned
on every outcome.

RunRecord schema v1 remains the exact historical #81 wire contract: it never serializes
`evidence_identity`, and reads or unrelated mutations do not migrate it. New runs use
schema v2 and may be unbound with `evidence_identity=None` and no evidence artefact.
Evidence binding explicitly transitions historical v1 or unbound v2 to bound v2; a
non-null identity requires exactly one corresponding manifest artefact. Future versions
fail explicitly. Before committing that pair in one ledger save, the service rereads
the typed persisted-artifact reference, hashes the exact
bytes, parses and semantically validates the manifest, checks the claimed identity and
artifact hash, and matches season/Gameweek to the run. Nonexistent, wrong, forged,
cross-acquisition and post-closure bindings are rejected.

No archive packaging, database, orchestrator, scenario lifecycle, optimiser, manager
state or unrelated authority/concurrency hardening was added. #84 remains responsible
for choosing and invoking operational acquisition/orchestration flows.

## Issue #82 implementation: deterministic `touchline doctor`

Implemented the deterministic, read-only `touchline doctor` diagnostics as the next
control-plane slice after #81/#83. GW1 rehearsal issue #73 exposed path-resolution
brittleness in the old runbook's Stage 6 `ensure_external_state_root` (a valid external
`STATE_ROOT` was rejected around `git rev-parse` / `is_relative_to` handling); #80's
state-root/path invariant is now encoded in the doctor's placement check.

## What changed

- New `application/doctor.py`: typed `DiagnosticCheck` (identifier, status, message,
  remediation), `DoctorReport`, `DiagnosticStatus` (PASS/WARN/FAIL) and a `DoctorService`
  whose probes (git runner, `which`, python version, required dirs, config path) are all
  injectable so tests never depend on the developer's machine.
- Twelve stable checks in fixed order: `tools.git`, `git.repository`, `git.commit_sha`,
  `git.release_tag`, `git.worktree_clean`, `paths.state_root_resolution`,
  `paths.state_root_exists`, `paths.state_root_placement`, `dirs.required`, `tools.uv`,
  `python.version`, `config.default_file`.
- `touchline doctor` CLI command (`--state-root`, `--json`); exit 0 only when no check
  failed, 1 otherwise. `application/__init__.py` exports the report/service seam for #84.
- Tests: 19 service-level (controlled tmp git repos, injected probes) + 5 CLI smoke
  tests, including the Stage 6 regression, symlink and equivalent-path cases, and a
  no-mutation test.

## Decisions

- **Release-isolation trigger** — the doctor treats the current checkout as an immutable
  release worktree only when HEAD is detached and exactly at a release tag
  (`git describe --exact-match --tags HEAD` ok AND `git symbolic-ref -q HEAD` fails).
  Branch checkouts at the same commit stay mutable development checkouts. When running
  from such a worktree, `paths.state_root_placement` requires the state root to resolve
  outside it; otherwise repository-local ignored state is permitted (matching the
  runbook's dev layout). Open tension: the GW1 runbook's "repository-local state/"
  wording predates the #80 release-isolation invariant; #84's documentation cut-over
  should reconcile it.
- **Canonical identity, never spelling** — placement compares `Path.resolve()` outputs
  (absolute, normalized, symlink-resolved) with `is_relative_to`; no string prefixes.
  `git status --porcelain` runs with `--no-optional-locks` so the doctor writes nothing.
- **Missing state root / required dirs are WARN, not FAIL** — every writer in the repo
  auto-creates its roots, so a missing directory never breaks a run; FAIL is reserved
  for genuine contract violations (placement inside a release worktree, missing tools,
  sub-3.12 Python, invalid YAML, non-repo).

## Risks / open questions

- The runbook/doctor tension above is the only policy judgment call; everything else
  follows #80/#82 literally. Flagged for the owner rather than re-decided.
- `uv` is treated as a required tool because every documented invocation is
  `uv run touchline ...`; if pip-installed operation is ever supported, `tools.uv`
  should be downgraded to informational.

## Issue #82 independent-review correction pass

Bounded correction pass after independent review, per the now-authoritative #80
confirmation that an immutable release worktree is exactly *detached HEAD at a
recognised release tag* (branch-at-tagged-commit and detached-at-untagged-commit are
ordinary mutable checkouts; dirty-worktree semantics unchanged: clean/dirty ordinary
checkout -> PASS/WARN, clean/dirty immutable release worktree -> PASS/FAIL).

- **Centralised state-root resolution** — `DoctorService.run()` now resolves the
  configured state root exactly once into a shared `_StateRootResolution`
  (canonical `Path` or explicit failure); `OSError`/`RuntimeError` (e.g. symlink
  loops) are caught centrally and the single result is consumed by all three
  `paths.*` checks. This removes the unprotected `.resolve()` calls in the existence
  and placement checks (which could previously crash `run()`) and the TOCTOU between
  independently re-resolved checks. A resolution failure is a deterministic FAIL with
  remediation in all three checks; the doctor never raises to the CLI for this class
  of invalid path input.
- **Existing-file consistency** — `paths.state_root_exists` no longer emits a
  misleading "does not exist" WARN when the state root resolves to an existing
  non-directory; it mirrors the resolution check's FAIL ("exists but is not a
  directory"). Resolution owns the type verdict; existence/placement consume the
  shared result.
- **Tests** — added regression coverage for: RuntimeError and OSError resolution
  failures yielding deterministic FAILs (single-resolution reuse verified by counting
  `Path.resolve` calls), existing-file-as-state-root consistency, and detached-HEAD
  without release tag treated as an ordinary checkout (clean -> PASS, dirty -> WARN,
  never FAIL). No existing Stage 6, symlink-containment, sibling-prefix,
  linked-worktree or release-worktree tests were weakened.

No policy semantics changed: the 12 stable check identifiers, deterministic order,
human/JSON contracts, zero-iff-no-FAIL exit, WARN semantics and read-only guarantees
are untouched.
