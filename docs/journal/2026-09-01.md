# 2026-09-01 — Touchline pause / re-entry snapshot

## Status

Touchline is intentionally deprioritised while attention moves to other projects. The project remains live; this is a pause point, not abandonment.

This note is a re-entry map rather than a complete project history. GitHub issues, merged PRs, ADRs, and prior journal entries remain authoritative for detailed decisions.

## Repository checkpoint

At pause time, current `develop` is headed by:

- `1dfb3281e875a9f9c018706add7efb38ab4e008b` — `Merge #98 captain-to-vice fallback`

Recent completed delivery includes:

- #73 control-plane epic and its bounded child stream completed and cut over;
- #114 ROLL / one-free-transfer candidate enumeration merged via PR #127;
- #92 immutable prospective lineup-evidence validation observations merged via PR #128;
- #93 official realised outcome joining / chronology safeguards implemented and merged;
- #94 lineup-evidence statistical evaluation merged;
- #98 captain-to-vice fallback optimisation implemented, independently reviewed, and merged.

## Known next work

#95 — `Generate reproducible lineup-evidence validation outputs` — remains open and is the natural next item in the #91 contemporary-lineup-evidence validation sequence after #92/#93/#94.

Do not automatically start #95 on re-entry. First verify current GitHub state and whether later commits/issues superseded this snapshot.

## Development governance

- Maximum two concurrent implementation worktrees.
- Parallel execution only across independently owned capability lanes.
- Each worktree has exclusive branch/agent ownership.
- Integration onto `develop` remains sequential.
- A new GitHub issue does not itself justify a new `08.xx` conversation.
- Prefer the narrowest existing authoritative Touchline conversation and finish/park existing implementation streams before creating new ones.

## Re-entry sequence

When Touchline becomes active again:

1. Fetch current `develop` and verify repository/worktree cleanliness.
2. Inspect open GitHub issues, PRs, local/remote feature branches, and `git worktree list` before assuming this snapshot is still current.
3. Reconcile or explicitly remove/park any abandoned feature worktrees before starting new implementation.
4. Re-read `00 — Touchline Project Index / Governance` for current conversation ownership and WIP rules.
5. Confirm whether #95 is still the correct next item in the lineup-evidence stream.
6. Prioritise operational Gameweek work only when a real deadline requires it; otherwise resume the narrowest approved backlog item.
7. Route completed implementation through `09.01 — Testing & Validation Framework` for independent acceptance where required.

## Completed work should stay closed

Do not reopen completed #73/control-plane work or other accepted capabilities merely to reconsider previous design choices. Reopen the narrowest relevant issue only when new operational evidence demonstrates a concrete defect or an authoritative dependency has changed.

## Restart principle

Before substantive work, establish current facts from Git/GitHub rather than relying on this dated snapshot. Resume from existing authoritative contracts and conversations; do not reconstruct or redesign completed work unnecessarily.
